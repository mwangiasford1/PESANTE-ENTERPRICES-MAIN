services:
  # Backend API (serves frontend in production)
  - type: web
    name: pesante-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && cd ../frontend && npm install && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URI
        value: mongodb+srv://pesante_admin:Asford254@project.ju5j3qr.mongodb.net/pesante_db?retryWrites=true&w=majority
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        value: pesante254
      - key: JWT_SECRET
        value: pesanteSuperSecretKey
      - key: CONTACT_EMAIL
        value: mwangiasford12@gmail.com
      - key: CONTACT_EMAIL_PASS
        value: mwangi2024
      - key: FRONTEND_URLS
        value: https://pesante-enterprices-main-1.onrender.com,http://localhost:5174

  # Frontend (with server for SPA routing)
  - type: web
    name: pesante-frontend
    env: node
    plan: free
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://pesante-backend.onrender.com/api